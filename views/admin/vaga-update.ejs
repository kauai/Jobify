   <%- include('cabecalho') %>
        <div class="container list-container">
          <div class="form-vaga">
            <h1>Atualizar Vaga</h1>
             <form method="post">
               <label>Titulo</label><br>
               <input value="<%- vaga[0].title %>" name="title"><br>
               <label>Descriaçao</label><br>
               <textarea name="desc"><%- vaga[0].desc %></textarea><br>
               <select name="categoria">
                  <% categorias.forEach(item => { %>
                     <option <%- vaga[0].categoria == item.id ? selected="selected" : ''%>
                     value="<%- item.id %>"><%- item.categoria %></option>
                  <% })%>
               </select><br>
               <button type="submit">Salvar vaga</button>
             </form>
          </div>
        </div>
    <%- include('footer') %>